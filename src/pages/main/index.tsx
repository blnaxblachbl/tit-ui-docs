import styled from "styled-components";

const Container = styled.div`
  display: flex;
  align-items: center;
`

const MainPage = () => {
  return (
    <Container>

    </Container>
  )
}

export default MainPage